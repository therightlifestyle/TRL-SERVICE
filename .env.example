# Canonical production origin used for metadata, canonical URLs, and the sitemap.
PUBLIC_SITE_URL="https://therightlifestyle.com"

# Gate 4 (contact endpoint) — not used by the Gate 3 static site.
# Real values are set only as encrypted environment variables in the hosting
# platform. Never commit real credentials.
# RESEND_API_KEY=""
# CONTACT_TO_EMAIL="officialtrlservice@gmail.com"
# RESEND_FROM_EMAIL=""
